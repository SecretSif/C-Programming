#include <stdio.h>

int main() {
char Name[10];
double salary,sales,total;
scanf("%s",&Name);
scanf("%lf",&salary);
scanf("%lf",&sales);
total=salary+(sales*15)/100;
printf("TOTAL = R$ %.2lf\n", total);

    return 0;
}
