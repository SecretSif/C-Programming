#include<stdio.h>
int main()
{
   int i,N=0,num;
   for(i=1;i<=5;i++)
   {
       scanf("%d",&num);
       if(num%2==0)
         {
             N++;
         }
   }
   printf("%d valores pares\n",N);
   return 0;
}
